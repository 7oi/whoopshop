@model Bordspil.Models.BJ

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>BJ</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.gameName)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.gameName)
            @Html.ValidationMessageFor(model => model.gameName)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.gameActive)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.gameActive)
            @Html.ValidationMessageFor(model => model.gameActive)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.numberOfPlayers)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.numberOfPlayers)
            @Html.ValidationMessageFor(model => model.numberOfPlayers)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.gameWinner)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.gameWinner)
            @Html.ValidationMessageFor(model => model.gameWinner)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
